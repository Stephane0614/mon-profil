import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplon.monprofil',
  appName: 'mon-profil',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
